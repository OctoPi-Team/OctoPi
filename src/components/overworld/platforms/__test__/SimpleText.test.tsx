import { SimpleText } from '../SimpleText';

test('mesh to have two children', async () => {
	// can be tested via react testing library
	<SimpleText position={[1, 2, 3]} textValue={'testText'} />;
});
